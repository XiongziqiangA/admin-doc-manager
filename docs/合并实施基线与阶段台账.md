# 合并实施基线与阶段台账

## 基线

| 系统 | 本地路径 | Git 基线 | 用途 |
| --- | --- | --- | --- |
| 行政资料管理系统 | `D:\Codex\2026-07-27（行政）\new-chat` | `eb89ec2` | 合并主平台 |
| AI 资产设备管理系统 | `C:\Users\Administrator\Documents\Codex\2026-08-24\z\outputs\ai-asset-management-system` | `cbaeeeb` | 迁移来源，保持只读 |

合并开发分支：`codex/enterprise-merge-foundation`

## 不可破坏的行政功能

每个阶段都必须回归以下能力：

- 登录和管理员/员工账号。
- 文件单个上传、批量上传和桌面拖放上传。
- OFD、PDF、Office、图片、压缩包等办公格式选择。
- 重复文件名提示和上传新版本。
- 文件分类、树形子分类、标签和移动分类。
- 文件详情、预览/打开、下载、打印、版本历史。
- 删除、回收站、恢复和永久删除。
- 文件数量统计、分页、搜索和批量导出 ZIP。
- 财务月度归集、候选文件、人工确认和交付 ZIP。
- AI 接口配置与连接验证。
- 项目事项、上下级、阶段、里程碑、风险问题、任务、跟进、合同、借款报销和附件关联。

## 阶段状态

| 阶段 | 内容 | 状态 | 验收证据 |
| --- | --- | --- | --- |
| 0 | 基线冻结、差异盘点、合并决策 | 已完成 | 两仓库状态、测试基线、ADR、数据盘点和合并边界已记录 |
| 1 | 统一组织、登录与权限基础 | 已完成 | Prisma 迁移、seed、权限测试、管理员登录及隔离运行回归通过 |
| 2 | 资产主数据与台账 | 已完成 | API、页面、资产唯一性、企业边界和真实分页测试通过 |
| 3 | 统一附件和文件知识能力 | 已完成 | 文件校验和、中文文件名、版本、关联、预览/下载和权限回归通过 |
| 4 | 资产流转与审批 | 已完成 | 状态机、事务、幂等、审批和通知接口回归通过 |
| 5 | 盘点、维修、异常与退出 | 已完成 | 生命周期、异常、维修、退出 API/页面和构建验证通过 |
| 6 | 统一工作台、搜索、通知和 AI | 已完成，AI 实际调用为可选项 | 工作台、文件中心、分类管理、项目事项、资产台账/流转、搜索、通知和 AI 状态均完成隔离运行验证；AI 未配置有效密钥时保持关闭，不阻断本地检索 |
| 7 | 数据迁移、桌面端切换和生产硬化 | 实现与隔离验收完成，上线部署待实施 | 迁移工具、seed、桌面双模式、Docker 镜像、容器烟测、备份恢复、依赖审计和生产脚本硬化均已验证；真实生产库 `--apply`、HTTPS 证书和防火墙属于上线前操作 |

## UI 目标

最终 UI 采用此前确认的统一企业工作台风格：深色左侧导航、顶部全局搜索、文件/资产/待办/财务指标、重点项目与资产状态、最近文件和待办提醒。该设计只调整信息架构和视觉呈现，不删减任何现有行政功能；原功能会归入对应一级模块或详情页签。

## 阶段 2 后端验收记录

- 资产类型、位置、资产台账、待确认资产和资产识别码模型已加入 PostgreSQL/Prisma。
- 资产列表支持真实总数分页，单页上限 100 不影响总文件或总资产数量。
- 正式资产创建、修改、详情、唯一约束、企业隔离和乐观锁已实现。
- 员工可提交待确认资产，但不能直接创建或修改正式资产；管理员确认过程使用单一数据库事务。
- 位置树支持新增、修改、停用和循环检测；仍有关联资产或启用子位置时禁止停用。
- 系统二维码随资产自动生成，人工识别码支持新增、修改、删除和主识别码唯一约束。
- 自动化测试共 31 个文件、164 项通过；全工作区类型检查和后端、前端、桌面端构建通过。
- 22 个历史迁移已在独立 PostgreSQL 17 临时数据库中完整执行，临时数据库随后删除，生产库未变更。

## 2026-09-11 阶段推进记录

- 后端回归测试：46 个测试文件、244 项通过。
- 全工作区 `pnpm typecheck` 通过；后端、前端、桌面端构建通过。前端只有 bundle 超过 500KB 的非阻断提示。
- 迁移工具测试：8 项通过；包括只读快照、SHA-256 校验、路径穿越、树形拓扑排序、标量外键读取、必需外键检查和 apply 预检。
- 旧资产真实 SQLite dry-run：2 个组织、5 个用户、2 个资产、3 条借用、1 个盘点任务、2 个审批、395 条审计日志；19 条提示均为旧 AI 历史不迁移。
- 迁移 `--apply` 已增加异常预检、单事务执行、数据库失败回滚和新复制附件清理；本轮没有对真实目标 PostgreSQL 执行 apply。

## 2026-09-12 隔离运行验收记录

- Docker Desktop/Linux Engine 已恢复，客户端和服务端版本均为 29.6.2。
- 使用 `docker-compose.acceptance.yml` 启动隔离 PostgreSQL，端口为 15432，数据仅写入 `data/acceptance-postgres`；未使用生产 8080 或生产 `data`。
- 当前 schema 的 30 个 Prisma 迁移全部成功执行，基础分类、管理员和企业基础数据 seed 成功。
- 当前源码构建的后端成功启动，`/api/health` 返回 200；网页端在 5173 端口登录成功，工作台、文件中心、分类管理、项目与事项、资产台账和资产流转页面均能加载。
- 登录、工作台、搜索、分类、文件、回收站、事项、资产、流转、盘点、维修、异常、退出、审批、通知、财务归集和 AI 状态读取接口均返回 200；空搜索词按接口校验返回 400，实际关键词返回 200。
- 首次访问 Docker Hub 的 BuildKit OAuth 因 IPv6 网络超时；随后切换到 `DOCKER_BUILDKIT=0`，成功拉取基础镜像并构建最新后端、前端镜像。
- 使用 `admin-docs-acceptance-postgres`、`admin-docs-acceptance-backend` 和 `admin-docs-acceptance-frontend` 启动隔离 Compose 栈，前端映射 `18080`，三个容器均 healthy。
- 通过 `http://localhost:18080` 完成容器化登录、10 个一级分类读取、OFD 上传、PDF 下载完整性、同名检测、财务 ZIP/中文清单、回收站恢复和永久删除验证。
- 在本地前端 `http://localhost:5173/` 的 `1440x900` 桌面视口完成工作台、文件中心、分类管理、项目与事项、财务归集、审批中心、AI 接口配置和回收站路由检查；页面级 `bodyScrollWidth` 不超过可视宽度，窄视口 `353px` 也无横向溢出。
- 隔离备份目录 `data/acceptance-backups/acceptance-20260912-093933` 完成数据库 dump 和存储归档恢复；源库与临时恢复库的 `documents:users:business_matters` 均为 `3:6:1`，存储归档均为 3 个文件、72 字节。
- 已用 `ExcelJS 4.4.0` 替换旧 `xlsx` 依赖；现代 XLSX/XLSM/XLTX/XLTM 内容索引和中文 `文件清单.xlsx` 可读，旧 XLS/XLSB 仍可上传、下载但不进行正文索引。
- 生产依赖审计结果为 `0 critical / 0 high / 0 moderate / 0 low`；旧历史镜像可能曾嵌入敏感环境信息，正式上线前必须轮换旧 API Key、数据库密码、管理员密码和 JWT 密钥。
- 本次未对真实生产库执行迁移工具 `--apply`，未修改旧资产系统或生产数据；隔离前后端进程和容器在验收结束后停止。
- 生产 Compose 已加入资源上限、`no-new-privileges`、只读根文件系统和临时文件系统；生产启动脚本会校验密码、JWT 密钥、默认管理员名和非本机 HTTP CORS 配置；备份脚本会校验 dump、storage 压缩包和 manifest。
